const ModesSelector = () => {
  // const { modes, changeActiveMode, activeMode } =
  //   useContext(VariablesContext)!;
  // if (activeMode === undefined || modes.length === 0) {
  //   return null;
  // }
  // const handleChange = (
  //   e: h.JSX.TargetedEvent<HTMLSelectElement, Event>
  // ): void => {
  //   const selectedCollection = (e.target as HTMLSelectElement).value; // Use type assertion here
  //   changeActiveMode(selectedCollection);
  //   e.preventDefault();
  // };
  // return (
  //   <select
  //     title={"Modes"}
  //     value={activeMode}
  //     className={styles.collectionSelectorBox}
  //     onChange={handleChange}
  //   >
  //     {modes.map((mode, index) => (
  //       <option key={mode} value={mode}>
  //         {mode}
  //       </option>
  //     ))}
  //   </select>
  // );

  return null;
};

export default ModesSelector;
